linode instance and firewall is managed with terraform

all that was done manually is manual teleport install and backrest install/setup

note: you can run 'tctl inventory ls --format=text' on the auth node to view all current versions