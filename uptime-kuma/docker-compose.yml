---
# MANAGED IN GIT, DO NOT EDIT IN PORTAINER
services:
  uptimekuma:
    image: louislam/uptime-kuma:2.4.0
    container_name: uptime-kuma
    networks:
      teleport_teleport:
    volumes:
      - ${DATA_DIR}:/app/data
    restart: always

networks:
  teleport_teleport:
    external: true
