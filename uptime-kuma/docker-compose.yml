---
services:
  uptimekuma:
    image: louislam/uptime-kuma:2.3.2
    container_name: uptime-kuma
    networks:
      teleport_teleport:
    volumes:
      - ${DATA_DIR}:/app/data
    restart: always

networks:
  teleport_teleport:
    external: true
